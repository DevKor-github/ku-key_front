import { css } from '@styled-system/css'

export const Wrapper = css({
  display: 'flex',
  flexDir: 'column',
  bgColor: 'bg.gray',
  justifyContent: 'center',
  alignItems: 'center',
  w: 'full',
  maxW: 1026,
})
